import { StyleSheet } from 'react-native';
import { SIZES } from '../../helpers/themes';

export const styles = StyleSheet.create({
  menuGrid: { marginTop: SIZES.medium, 
  //  paddingHorizontal: SIZES.small 
  },
});
